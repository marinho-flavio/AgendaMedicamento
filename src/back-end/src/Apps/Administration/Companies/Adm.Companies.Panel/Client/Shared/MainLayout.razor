@inherits LayoutComponentBase

<div class="page">
    <HeaderDisplay />
    <Companies.Adm.Panel.Client.Shared.Menu.NavMenu OnMenuActivated="(val) => {this.isSubMenuActive = val; StateHasChanged();}" />
    <main>
        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@code {
    public bool isSubMenuActive { get; set; }
}